# API Configuration
PORT=3000
API_PREFIX=api
NODE_ENV=development

# Database Configuration (para cuando implementes una base de datos)
# DATABASE_URL=postgresql://postgres:password@localhost:5432/redsismica
# DATABASE_HOST=localhost
# DATABASE_PORT=5432
# DATABASE_USERNAME=postgres
# DATABASE_PASSWORD=password
# DATABASE_NAME=redsismica

# JWT Configuration (para autenticación)
# JWT_SECRET=your_jwt_secret_key
# JWT_EXPIRATION=1d
